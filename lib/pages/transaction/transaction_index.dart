import 'package:flutter/material.dart';

// 거래추가
class TransactionIndex extends StatelessWidget {
  const TransactionIndex({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('거래추가'),);
  }
}
