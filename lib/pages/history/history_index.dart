import 'package:flutter/material.dart';

// 거래내역

class HistoryIndex extends StatelessWidget {
  const HistoryIndex({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('거래내역'),);
  }
}
